Grafik erstellen:

- Histogramme wie in PDF (max(p_i/q_i, q_i/p_i)

Tabelle erstellen:

- TVD für die vier Methoden

- Bestimmung von c

- Behauptung von min(l_ip, l_i2) * 0.5 <= w_i <= max(l_ip, l_i2) * 2